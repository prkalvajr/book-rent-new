apiVersion: v1
kind: Service
metadata:
  name: bookrent-api
  namespace: bookrent
  labels:
    app.kubernetes.io/name: bookrent-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bookrent-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
